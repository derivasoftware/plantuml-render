# Argos runtime state (federation cache, eval data, reports) — derived,
# never committed.
.argos/

# Per-operator workspace overrides (REQ-00625-1): the GDD analogue of
# an editable install. Always local, never committed.
argos.local.toml
node_modules/
out/
dist/
*.svg
.venv/
src/render/schema.gen.ts
src/render/validate.gen.js
src/render/validate.gen.d.ts
test-results/
!doc/assets/*.svg
src/render/version.gen.ts
